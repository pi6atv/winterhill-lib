package commands

// SendWhShort sends a WhShort type command to the specified host and port
func SendWhShort(command WhShort, host string, port int64) error {
	// connect to remote
	// render command into string
	// send it
	return nil
}
